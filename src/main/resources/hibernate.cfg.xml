<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE hibernate-configuration SYSTEM "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>
        <property name="hibernate.dialect">org.hibernate.dialect.PostgresPlusDialect</property>
        <property name="hibernate.connection.driver_class">org.postgresql.Driver</property>

        <!-- Assume test is the database name -->
        <property name="hibernate.connection.url">jdbc:postgresql://localhost:5432/test_database</property>
        <property name="hibernate.connection.username">postgres</property>
        <property name="hibernate.connection.password" />

        <!-- List of XML mapping files -->
        <!--<mapping resource="r"/>-->

        <property name="cache.use_query_cache">true</property>

        <property name="cache.use_second_level_cache">true</property>

        <!-- Generate L2 cache statistics. -->
        <property name="generate_statistics">true</property>

        <!-- Specify GridGain as L2 cache provider. -->
        <property name="cache.region.factory_class">org.apache.ignite.cache.hibernate.HibernateRegionFactory</property>

        <!-- Specify the name of the grid, that will be used for second level caching. -->
        <property name="org.apache.ignite.hibernate.grid_name">hibernate-grid</property>

        <!-- Set default L2 cache access type. -->
        <property name="org.apache.ignite.hibernate.default_access_type">READ_ONLY</property>

        <!-- Specify the entity classes for mapping. -->
        <mapping class="ru.innopolis.server.entity.StudentsEntity"/>
        <mapping class="ru.innopolis.server.entity.LessonsEntity"/>
        <mapping class="ru.innopolis.server.entity.StudentsLessonEntity"/>

        <!-- Per-class L2 cache settings. -->
        <class-cache class="ru.innopolis.server.entity.StudentsEntity" usage="read-only"/>
        <class-cache class="ru.innopolis.server.entity.LessonsEntity" usage="read-only"/>
        <class-cache class="ru.innopolis.server.entity.StudentsLessonEntity" usage="read-only"/>

    </session-factory>

</hibernate-configuration>